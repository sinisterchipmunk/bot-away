require 'spec_helper'

describe "ERB templates" do
  # For a future version. For now, ignore specific items independently using ActionController::Request.accepts_unfiltered_params.
#  obfuscates("checked check_box_tag", :name => "user[role_ids][]") do
#    builder.send(:template).check_box_tag("user[role_ids][]", 1, true)
#  end
end

